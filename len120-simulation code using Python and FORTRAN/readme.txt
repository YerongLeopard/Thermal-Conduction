"crystal.f90" is the main program;
"slab.dat" is the document recording temperatures of each slab at every single step;
"error.dat" is the document recording errors of each slab at every single step;
"run.pbs" is the PBS script;
"plotting.py" is written in python show you the temperature profile of the results from the simulation.